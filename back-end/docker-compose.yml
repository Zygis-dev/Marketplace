version: "3.8"

services:
  db:
    image: mysql:8.0
    container_name: marketplace_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: marketplace
    ports:
      - "3306:3306"
